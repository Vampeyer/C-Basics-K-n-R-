#include <stdio.h>

int main()
{
int c;

while ((c = getchar()) != EOF )
	putchar(c);
}

/* This is a simplified version of the thile loop.
note , that c = getchar() , is a value of which,
is defined on the left side , after getchar , is assigned to c.

As it is defined , it is , a value representation , within itself.

- and therefore wrapped in parenthesis ,to represent its representational value.

 */
